# Copy this file to relay.env (gitignored) and `source` it -- same content
# on every machine that talks to the registry through the relay. See
# RUN_TRACKING.md's "Working across machines" section.
#
# This only works out of the box if `oceanicu-relay` is defined as a
# Host alias in THIS machine's ~/.ssh/config -- that's where the actual
# per-machine differences (username, IdentityFile, ProxyJump, ...)
# belong, not here. Example ~/.ssh/config stanza:
#
#   Host oceanicu-relay
#       HostName bb-server1
#       User your-username-on-the-relay
#
# Without such an alias, replace `oceanicu-relay` below with a literal
# user@relay-host instead -- see RUN_TRACKING.md for why the alias form
# is recommended over that.

export OCEANICU_RUN_DB=ssh://oceanicu-relay/data/OceanICU/oceanicu_3d/experiments/run_registry.sqlite
export OCEANICU_RELAY_DIR=/data/OceanICU/oceanicu_3d/experiments

# Short commands (oceanicu-runs, chunk-runner) from anywhere -- e.g. from
# inside a run's own chunk directory, without the full path to
# oceanicu_3d/scripts/*.py every time. Same repo, same path on every
# machine (see RUN_TRACKING.md), so this line doesn't need to change
# per-machine either.
export PATH="$HOME/source/repos/OceanICU/oceanicu_3d/scripts/bin:$PATH"
