# Generated by setup_fake_runs.py -- regenerable, not source.
test_registry.sqlite
test_registry.sqlite-wal
test_registry.sqlite-shm
runs/

# Written by `oceanicu_runs.py delay-all` -- a live runtime sentinel, not source.
DELAY_ALL

# Written by run_tracking.py's own DB-backup mechanism (a nested git repo
# holding periodic snapshots) -- runtime output, not source, and a nested
# .git would confuse tooling if left tracked.
test_registry.sqlite.backups/
.test_registry.sqlite.backup_write_count

# Created by trying the "Trying the command queue risk-free" README
# walkthrough -- runtime test output, not source.
hpc_commands/
